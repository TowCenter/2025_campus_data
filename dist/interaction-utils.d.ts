/**
 * Gets the interaction type from a row
 * @param {any} interaction - Interaction value
 * @returns {string} Normalized interaction type
 */
export function getInteractionType(interaction: any): string;
export namespace INTERACTION_TYPES {
    let LAWSUIT: string;
    let GRANT: string;
    let DEAL: string;
}
